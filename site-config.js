/* The only contact settings you need to edit before launch.
 * Never put a private API key in this file: every file here is public.
 */
window.SB_CONFIG = Object.freeze({
  contactEmail: "",          // Set your real receiving address to enable email drafts.
  inquiryEndpoint: "",       // Optional HTTPS/same-origin POST endpoint; see README.
  founder: {
    name: "",               // This section stays hidden until a name is supplied.
    bio: "",                // Plain text only. Use actual, approved background.
    profileUrl: ""
  }
});
